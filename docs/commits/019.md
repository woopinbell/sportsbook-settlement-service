# 019 — docs(readme): 성능 섹션 박제

## 개요
실측 처리량을 README "성능" 섹션에 박제. 면접관이 README만 봐도 숫자와 정직한 한계를 알 수 있게.

## 작업 내역
- README 성능 섹션을 placeholder("측정 후 박제")에서 실측 표로 교체:
  - LOST 엔진 915 bets/s, WON full-payout 589 bets/s (dev-host, 1만, 32스레드).
  - 엔진은 목표치 근접, wallet HTTP 2회가 WON throughput 결정.
  - dev-host 미달 이유 + production 확장 축(Kafka partition / 다중 인스턴스 / 풀링 wallet) — betting의 "dev-host vs production target" 톤 유지.
  - 중복 payout 0(핵심 불변식): `outbox.count()==N` + 16스레드 동시성 증명 + betId 멱등 키.
- README 단독 변경이므로 `docs(readme):` 단독 커밋(dev 커밋과 안 섞음).

## 결과
README가 측정 결과 + 한계를 자체완결적으로 전달.

## 요약
- 성능 표 + 정직한 production 캐비엇 박제.

## 다음 작업
(dev 종료) → 사용자 push → retrospective.

## 핵심 확인
README "성능" 섹션 + `load-test/results/BEST.md`.

## 기억/설명 Level
- **L1**: README에 숫자 있음.
- **L2**: 왜 docs(readme) 단독 커밋인가(CLAUDE.md 정책 — README 후속 변경 분리).
- **L3**: (없음)
